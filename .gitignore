# Puertos / overrides de Docker Compose en la raíz (no subir preferencias por máquina)
/.env

/backend/vendor
/backend/.env
/backend/.env.backup
/backend/.env.production
/backend/storage/*.key
/backend/node_modules
/frontend/node_modules
/frontend/dist
*.log
.DS_Store
Thumbs.db

# Volcados locales de MySQL (pueden contener datos sensibles)
/backups/

# Seeder de datos ficticios solo para máquinas locales (no versionar)
/backend/database/seeders/DevFakeDataLocalSeeder.php

# Scripts de arranque y utilidades locales (solo para la máquina del desarrollador)
/HBM
/HBM.ps1
/HBM.cmd
/HBM-Levantar.cmd
/HBM-UsuariosDev.cmd

# Carpetas de herramientas de IA / IDE (no son código del proyecto)
/.claude/
/.cursor/

# Planes y documentación interna de desarrollo
/Planes/
