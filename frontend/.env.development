# Vacío: en dev las llamadas van a /api/* y Vite las reenvía (evita CORS localhost vs 127.0.0.1).
# Con Docker Compose, el servicio frontend define VITE_API_BASE_URL y VITE_API_PROXY_TARGET
# (en el contenedor el proxy debe apuntar a http://nginx, no a 127.0.0.1).
VITE_API_BASE_URL=
# Frontend en tu máquina + API en Docker (puerto 8080 publicado):
VITE_API_PROXY_TARGET=http://127.0.0.1:8080
